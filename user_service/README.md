# User Service

Minimal user service for Phase 2. Runs on port 5100 by default.

Endpoints:
- GET / -> service info
- GET /health -> health check
- GET /users -> list users from ../data/users.json

Run: `python app.py`